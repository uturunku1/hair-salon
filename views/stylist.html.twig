<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Hair Salon</title>
    <link rel="stylesheet" href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css'>
    <!-- <link rel="stylesheet" href="/styles.css" media="screen" title="no title" charset="utf-8"> -->

  </head>
  <body>
    <h2>{{stylist.getName}}</h2> <a href="/stylist/{{stylist.getId}}/edit">Edit/remove the name of this stylist.</a>
    <p></p>

    <form action="/stylist/{{stylist.getId}}" method="post">
      <input type="hidden" name="stylist_id" value="{{stylist.getId}}" required>
      <label for="name">Enter a client's name:</label>
      <input type="text" name="name" required>
      <button type="submit">Add</button>
    </form>

    {% if clients is not empty %}
        <ul>
          {% for client in clients %}
          <li>{{ client.getName }}
            <form class="solo-btn" action="/stylist/{{stylist.getId}}" method="post">
              <input type="hidden" name="_method" value="delete">
              <input type="hidden" name="client_id" value="{{client.getId}}">
              <button class="btn btn-danger" type="submit">Delete</button>
              <a class="btn btn-warning" href="/client/{{client.getId}}/edit">Edit</a>
            </form>
          </li>
          {% endfor %}
        </ul>
    {% endif %}</br>


    <a href="/">Return Home</a>
  </body>
</html>
